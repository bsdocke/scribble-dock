var function = init(){
	addHeaderButtonListeners();
	addRibbonButtonListeners();
};

var function = addHeaderButtonListeners(){
	addFileButtonListener()P
};

var function = addFileButtonListener(){
	var fileBtn = document.getElementById("file_btn");
	filebBtn.addEventListener("onclick", onFileBtnClickListener, true);
};

var function = onFileBtnClickListener(e){
	
};
